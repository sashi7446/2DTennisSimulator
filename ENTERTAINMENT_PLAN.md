# 楽しませる計画 (Entertainment Plan)

このプロジェクトの目的は「AIが勝手に賢くなっていく様子を神視点で楽しむ」こと。
だが現状スクリーンに映っているのは **動く点2つとボール、それと報酬グラフ** だけだ。
シミュレータとしては正しく動いているが、**観戦コンテンツとしては素材のまま** である。

この計画は「機能を増やす」ためではなく、**眺めている人間の時間を面白くする**ために組んである。

---

## 楽しさの3軸

| 軸 | 問い | 現状 |
|----|------|------|
| **物語 (Narrative)** | 誰が強い？ 昨日より強くなった？ | 勝率カウンタのみ。積み上がる物語がない |
| **手触り (Feel)** | 見ていて気持ちいい？ | 無音・無エフェクト。ラリーの興奮が伝わらない |
| **介入 (Agency)** | 自分は関われる？ | 完全に傍観者。速度変更と一時停止のみ |

以下、**効果 ÷ 実装コスト** の高い順に3フェーズで並べる。

---

## Phase 1: 週末で効く即効性 (Quick Wins)

「まず今週末、起動した瞬間に体感が変わる」もの。既存の構造にほぼ乗るだけで済む。

### 1.1 ラリーカウンター & 実況ログ

| 項目 | 内容 |
|------|------|
| 難易度 | ★☆☆☆☆ |
| 効果 | ★★★★☆ (ラリーに意味が生まれる) |
| 関連ファイル | `stats_tracker.py`, `renderer.py`, `main.py` |

`Game` はすでに `rally_count` を持ち、`StepResult.hit_occurred` と `PointResult.reason`
(`"in"` / `"out"`) を返している。**素材は全部揃っていて、誰も画面に出していない**だけ。

**実装手順:**

- [ ] `StatsTracker` にラリー統計を追加
  - [ ] `longest_rally: int` / `current_rally: int` / `rally_history: List[int]`
  - [ ] `end_episode()` でラリー長を記録
- [ ] `main.run_visual_game()` のループで `result.hit_occurred` / `result.point_result` を
      `stats.log_event()` に流す
  ```python
  if any(result.hit_occurred):
      hitter = "A" if result.hit_occurred[0] else "B"
      stats.log_event(f"{hitter} リターン (ラリー {game.rally_count})")
  if result.point_result:
      pr = result.point_result
      stats.log_event(f"ポイント {'AB'[pr.winner]}! ({pr.reason})")
  ```
- [ ] 新規 `CommentaryOverlay` (`Overlay` Protocol 準拠) でコート横に直近8件を表示
- [ ] **最長ラリー更新時は画面中央にフラッシュ表示** — 「NEW RECORD: 23 RALLIES」
      これが一番効く。記録更新は無条件で気持ちいい

**完了条件:** デバッグ表示なしの通常モードでも、ラリー数と決着理由が読める。

---

### 1.2 打球音とヒットエフェクト

| 項目 | 内容 |
|------|------|
| 難易度 | ★★☆☆☆ |
| 効果 | ★★★★☆ (体感が一段変わる) |
| 関連ファイル | `renderer.py`, 新規 `audio.py` |

無音のゲームは驚くほどつまらない。逆に **音が付いた瞬間に「試合」になる**。
音源ファイルを持ちたくないなら、`numpy` で矩形波・ノイズを生成して
`pygame.sndarray` に食わせればアセットゼロで済む(このリポジトリは numpy 依存済み)。

**実装手順:**

- [ ] `audio.py` — 波形生成でSEを合成
  - [ ] ヒット音 (短いアタック音、打球速度でピッチ変化)
  - [ ] 壁ヒット音 / ポイント決着音
  - [ ] `--mute` CLIオプションと、pygame.mixer 初期化失敗時の無音フォールバック
- [ ] `renderer.py` にヒット時のエフェクト
  - [ ] 打点に一瞬の白い円 (数フレームで消える)
  - [ ] ポイント決着時の軽い画面シェイク (描画オフセットを2〜3px揺らすだけ)
- [ ] ヘッドレスモードでは絶対に鳴らさない・初期化もしない

**注意:** `Renderer` は「渡されたものを描くだけ」の受動層(ARCHITECTURE.md)。
エフェクト状態は `StatsTracker` 側かエフェクト専用の小さな状態オブジェクトに置き、
描画層に持ち込まないこと。

---

### 1.3 ハイライトリプレイ

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★☆☆ |
| 効果 | ★★★★★ (「今の見た!?」を再生できる) |
| 関連ファイル | 新規 `replay.py`, `main.py`, `input_handler.py` |

**神視点の醍醐味は「巻き戻せること」**。物理状態は数値が十数個しかないので、
リングバッファに毎フレーム積むだけでリプレイになる。動画エンコード不要。

**実装手順:**

- [ ] `replay.py` — `deque(maxlen=600)` (=10秒) に `game.get_observation()` を毎フレーム記録
- [ ] 長いラリー / 記録更新ポイントの直後に自動でリプレイ再生
  - [ ] 0.3倍速、画面隅に「REPLAY」表示
- [ ] `input_handler` に手動キー割り当て (`[` = 直近リプレイ再生)
- [ ] リプレイを JSON で保存 → `--replay <file>` で再生できるように
- [ ] (発展) 保存したリプレイから GIF 書き出し → TASK_LIST の「デモGIF追加」がタダで片付く

**完了条件:** 20ラリー超のポイントが決まった直後、自動でスロー再生が入る。

---

## Phase 2: 物語をつくる

観るたびに違う結果が出るだけでは飽きる。**積み上がる記録**があると人は戻ってくる。

### 2.1 トーナメント & Eloレーティング

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★☆☆ |
| 効果 | ★★★★★ (「最強は誰か」という物語の土台) |
| 関連ファイル | 新規 `tournament.py` (TASK_LIST #10 と合流) |

**実装手順:**

- [ ] `tournament.py` — 総当たり戦。既存の `run_headless_training` を1試合単位で再利用
- [ ] Elo レーティング計算 (K=32、初期1500)
- [ ] 結果を `tournament/results/*.json` に追記保存 — **過去のレーティング推移が残ることが重要**
- [ ] CLI: `python main.py --mode tournament --agents chase,smart,neural,transformer --matches 100`
- [ ] 順位表をターミナルに表形式で出力 (勝敗表 + Elo + 平均ラリー長)
- [ ] (発展) 決勝のみ pygame で観戦モード再生

**なぜ効くか:** ヘッドレスで全試合を回し、**決勝だけ観る**という構造は、
「全部見なくていい」ので継続しやすい。

---

### 2.2 世代セルフプレイと「過去の自分」との対戦

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★★☆ |
| 効果 | ★★★★★ (成長がドラマになる) |
| 関連ファイル | 新規 `selfplay.py`, `agents/` |

保存・読み込み機能はすでにある(`--save-dir`)。あとは **世代を回す仕組み**だけ。

**実装手順:**

- [ ] `selfplay.py` — N エピソードごとにスナップショットを `gen_001`, `gen_002`... と保存
- [ ] チャンピオンプール: 過去世代からランダムに対戦相手を選ぶ (戦略の忘却対策)
- [ ] 昇格判定: 現世代が旧チャンピオンに勝率55%超で **チャンピオン交代**
- [ ] 世代交代のたびにログへ 1行 — 「Gen 12 が Gen 8 を 61% で撃破、王座交代」
- [ ] `--gauntlet gen_001,gen_005,gen_010` で全世代総当たり → **成長曲線が勝敗表として出る**

**観戦の見せ場:** `python main.py --agent-a saved/gen_050 --agent-b saved/gen_001`。
「50世代目 vs 初代」を並べて観るのが、このプロジェクトで一番面白い画になるはず。

---

### 2.3 ヒートマップ・オーバーレイ

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★☆☆ |
| 効果 | ★★★★★ (プロジェクトの根本テーマの答え合わせ) |
| 関連ファイル | `renderer.py`, `stats_tracker.py` |

README が掲げている問い —— **「ホームポジションに戻る戦術は自然発生するか」**。
これに答える画が、まだどこにもない。ヒートマップはその答えを一枚の絵にする。

**実装手順:**

- [ ] `stats_tracker.py` に位置グリッド集計 (例: 40×20 の 2D ヒストグラム)
  - [ ] 各プレイヤーの滞在位置を毎フレーム加算
  - [ ] 「打った瞬間の位置」と「待機中の位置」を分けて集計すると解像度が上がる
- [ ] `HeatmapOverlay` (`Overlay` Protocol 準拠) — コート上に半透明カラーマップ
- [ ] `H` キーでトグル、A/B切り替え
- [ ] 学習前後の比較モード: `gen_001` と `gen_050` のヒートマップを左右に並べる
- [ ] 集計結果を PNG 出力 → README に貼る

**完了条件:** 「初代は全面をうろつく / 50世代目は中央に戻る」が一目で分かる図が出せる。

---

## Phase 3: 参加させる

### 3.1 人間 vs AI モード

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★☆☆ |
| 効果 | ★★★★★ (傍観者から当事者へ) |
| 関連ファイル | 新規 `agents/human.py`, `input_handler.py`, `main.py` |

エージェントは「obs を受け取り action を返すブラックボックス」(ARCHITECTURE.md)。
**人間もその契約を満たせる。** `HumanAgent` を1つ足すだけで対戦モードになる。

**実装手順:**

- [ ] `agents/human.py` — `Agent` を実装し、`InputHandler` から現在のキー状態を読む
  - [ ] 移動: 矢印キー / WASD → 16方向 (`movement` 0-15, 静止16) にマップ
  - [ ] 打球角度: マウス位置 or 左右キーで狙う方向を指定
  - [ ] `learn()` は no-op
- [ ] `InputHandler` に押しっぱなし対応 (`pygame.key.get_pressed()`) を追加
      — 現状は KEYDOWN イベントのみで、操作には向かない
- [ ] `--agent-a human` で起動できるように `create_agent()` へ登録
- [ ] ヘッドレスモードで human を指定したら明確にエラー

**副次効果:** 自分で操作すると **AIの強さが体感で分かる**。
「gen_050 に勝てない」が最高のデモになる。

---

### 3.2 マルチポイント・タイブレーク

| 項目 | 内容 |
|------|------|
| 難易度 | ★★★★☆ |
| 効果 | ★★★☆☆ |
| 関連ファイル | `game.py`, `env.py` (TASK_LIST #9 と合流) |

1ポイント=1エピソードだと **試合の緊張感がない**。
「6-6 からのタイブレーク」という構造だけで観戦の密度が変わる。
ただし observation 拡張は学習側に影響するので、Phase 1/2 が終わってから着手する。

---

### 3.3 試合サマリー出力

| 項目 | 内容 |
|------|------|
| 難易度 | ★★☆☆☆ |
| 効果 | ★★★☆☆ |
| 関連ファイル | 新規 `summary.py` |

- [ ] 試合終了時にサマリーを Markdown/HTML で出力
  - [ ] スコア推移、最長ラリー、決着理由の内訳 (in / out の比率)
  - [ ] ヒートマップ画像を埋め込み
- [ ] `--report out.md` オプション
- [ ] ヘッドレス長時間学習の結果を **後から眺めて楽しめる**ようにする

---

## 推奨作業順序

```
1.1 ラリーカウンター/実況   ← まずこれ。1日で体感が変わる
        ↓
1.2 音とエフェクト          ← 「試合」になる
        ↓
2.3 ヒートマップ            ← プロジェクトの問いに答える画が出る
        ↓
1.3 ハイライトリプレイ      ← ここでGIFも撮れてREADMEが強くなる
        ↓
2.1 トーナメント/Elo        ← 物語の土台
        ↓
2.2 世代セルフプレイ        ← 最大の見せ場「50世代目 vs 初代」
        ↓
3.1 人間 vs AI              ← 当事者になる
        ↓
3.2 / 3.3 拡張
```

---

## 最初の一手

**1.1 のラリーカウンターだけ実装して、20ラリー超えたら画面を光らせる。**

必要なデータ (`rally_count`, `hit_occurred`, `PointResult.reason`) は
すでに `Game` が返している。読んで、描くだけ。

これだけで、今まで「点が往復しているだけ」だった画面が
「今のラリー長い!」と声が出る画面になる。そこから先は上の順に積む。

---

## 設計上の約束

この計画のどれを実装するときも、以下は崩さないこと (ARCHITECTURE.md 準拠)。

| 原則 | 意味 |
|------|------|
| Renderer は受動 | 演出の状態を描画層に持たせない。渡されたものを描くだけ |
| StatsTracker は描画から独立 | ヘッドレス学習でも同じ統計が取れること |
| Agent は Config に依存しない | `HumanAgent` も例外ではない |
| ヘッドレスを重くしない | 音・エフェクト・リプレイ記録は可視モード限定 |
